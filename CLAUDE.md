# CLAUDE.md

# ⛔ 소스 코드는 절대 수정하지 않는다

**이 저장소의 소스 코드는 오직 사람(Jaehyeok)만 작성하고 수정한다.**
**너는 읽고, 검토하고, 제안만 한다.**

개인 포트폴리오 프로젝트이고, 평가받는 대상이 코드가 아니라 **사람의
엔지니어링 판단력**이기 때문이다. 네가 코드를 쓰면 이 프로젝트의 존재 이유가
사라진다.

**금지** (사람이 그 대화에서 명시적으로 시키지 않는 한)
- `*.cpp` `*.h` `*.hpp` `*.vcxproj` `*.sln` `*.filters` 생성·수정·삭제
- 포매팅·리네임·"겸사겸사" 리팩터링
- `git commit` `push` `merge` `rebase` `reset` 등 git 쓰기 작업

**허용**
- 읽기·검색·탐색 전부, `git log`/`diff`/`status`
- 빌드·테스트 실행 — **결과 관찰 목적으로만**
- `docs/**` 및 `*.md` 작성 — 문서는 네가 관리한다
- 패치를 **대화나 문서에** 코드 블록으로 제시 (파일에 쓰지 않는다)

**"어차피 한 줄인데", "명백한 오타인데", "빌드가 깨져 있는데"는 예외가 아니다.**
올바른 응답은 "고쳐뒀어"가 아니라 **"이렇게 고치면 돼"** + 코드 블록이다.

---

## 나머지 전부는 AGENTS.md에 있다

**[AGENTS.md](AGENTS.md)를 지금 읽어라.** 불변식(I-1~I-8), 코딩 관례,
코드 지도, 검토 규칙, 문서 규칙이 전부 거기 있다. 이 파일은 위의 절대 규칙이
어떤 경우에도 누락되지 않게 하려고 따로 둔 것이다.

작업 전에 함께 볼 것:
- [docs/wiki/decisions.md](docs/wiki/decisions.md) — **제안하기 전에.** 이미 버려진 안일 수 있다
- [docs/wiki/review-log.md](docs/wiki/review-log.md) — 미해결 지적. 중복 지적을 피하려면 먼저 읽어라
- [docs/superpowers/plans/](docs/superpowers/plans/) — 진행 중인 실행 계획
